{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dodawanie produktu</h1>

    {{ form_start(form) }}
        {% form_theme form 'form/fields.html.twig' %}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('product_index') }}">Wstecz</a>
        </li>
    </ul>
{% endblock %}
